<% if current_user %>
  <nav class="flex justify-between lg:justify-around p-4 top-shadow-lg md:shadow-lg z-50">
    <div class="hidden md:block">
      <%= render partial: 'layouts/menu/logo' %>
    </div>
    <div class="flex justify-around gap-2 md:gap-6 w-full md:w-auto">
      <%= render partial: 'layouts/menu/menu_item', locals: {
        path: spaces_path,
        icon: 'search',
        text: t('menu.find_spaces')
      } %>
      <%= render partial: 'layouts/menu/menu_item', locals: {
        path: "https://om.lokaler.lnu.no",
        icon: 'knowledge',
        text: t('menu.about')
      } %>
      <%= render partial: 'layouts/menu/menu_item', locals: {
        path: edit_session_path,
        icon: 'account',
        text: t('menu.account')
      } %>
    </div>

  </nav>
<% end %>
