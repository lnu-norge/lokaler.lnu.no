<h1>Editing field '<%= @field %>' on <%= @space.title %></h1>
<%= turbo_frame_tag "#{dom_id(@space)}-#{@field}" do %>
  <%= simple_form_for @space do |form| %>
    <%= edit_field @field, form %>
    <%= form.button :submit %>
  <% end %>
<% end %>
