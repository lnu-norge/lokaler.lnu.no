
  <div class="max-w-md w-full space-y-8">
    <div>
      <%= image_tag 'logo.svg', alt: 'LNU logo', width: 200, height: 200, class: 'mx-auto h-12 w-auto' %>
      <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
        Logg inn til din bruker
      </h2>
      <p class="mt-2 text-center text-sm text-gray-600">
        eller
        <%= link_to 'registrer deg her', new_registration_path(resource_name), class: 'font-medium text-lnu-pink hover:text-lnu-blue' %>
      </p>
    </div>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), class: 'mt-8 space-y-6') do |f| %>
      <input type="hidden" name="remember" value="true">
      <div class="rounded-md shadow-sm -space-y-px">
        <div>
          <%= f.label :email, 'E-post', class: 'sr-only' %>
          <%= f.email_field :email, autofocus: true, autocomplete: 'email', class: 'emailField', placeholder: 'epost@epost.no', autofocus: true, required: true %>
        </div>
        <div>
          <%= f.label :password, 'Passord', class: 'sr-only' %>
          <%= f.password_field :password, autocomplete: "current-password", class: 'passwordField fieldLast', placeholder: '******', required: true %>
        </div>
      </div>
      <div class="flex items-center justify-between mt-4">
        <div class="flex items-center">
          <% if devise_mapping.rememberable? %>
            <%= f.check_box :remember_me, class: "h-4 w-4 text-lnu-pink focus:ring-lnu-pink border-gray-300 rounded" %>
            <%= f.label :remember_me, 'Husk meg', class: 'ml-2 block text-sm text-gray-900' %>
          <% end %>
        </div>
        <div class="text-sm">
          <%= link_to 'Glemt passordet ditt', new_password_path(resource_name), class: 'font-medium p-2 rounded text-lnu-blue hover:text-lnu-pink focus:outline-none focus:ring-2 focus:outline-ring-offset-2 focus:ring-lnu-pink' %>
        </div>
      </div>
      <div class="mt-4">
        <%= button_tag :submit, class: 'group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-lnu-pink hover:bg-lnu-blue focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-lnu-pink' do %>
          <span class="absolute left-0 inset-y-0 flex items-center pl-3">
            <!-- Heroicon name: solid/lock-closed -->
            <svg class="h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd" />
            </svg>
          </span>
          Logg inn
        <% end %>
      </div>
    <% end %>
  </div>
