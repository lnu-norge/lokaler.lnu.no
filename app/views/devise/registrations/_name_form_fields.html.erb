<%# locals: (form: ) %>
<fieldset>
  <legend class="text-md font-semibold tracking-wide">Hva heter du?</legend>
  <div class="flex flex-col md:flex-row md:gap-2 justify-items-stretch align-baseline">
    <%= form.input :first_name, autofocus: true, wrapper_html: { class: "flex-1" }, input_html: { class: 'fieldFirst' }, required: true %>
    <%= form.input :last_name, wrapper_html: { class: "flex-1"}, input_html: { class: 'textField' } %>
  </div>
</fieldset>